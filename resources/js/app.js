import './bootstrap';
import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

// Zde už není žádná logika pro ořezávání obrázku.
// Alpine.js a Bootstrap jsou nutné pro fungování rozhraní z Laravel Breeze.